

<view class="count" wx:if="{{ data.type == 1 }}">
  <van-tag round type="warning">3</van-tag>
</view>
<view class="img">
  <van-image
  width="60"
  height="60"
  fit="contain"
  src="{{ data.img }}"/>
  <view class="name">{{ data.name }}</view>
</view>